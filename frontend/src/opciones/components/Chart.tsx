import { Chart as ChartJS } from "chart.js";
import { Line } from "react-chartjs-2";
import { OptionResult } from "../../types";

export const Chart = ({ data }: { data: OptionResult[] }) => {
  return <></>;
};
